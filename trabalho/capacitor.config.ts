import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TRABALHO DO JOAO',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
